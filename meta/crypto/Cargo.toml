[package]
name = "crypto"
version = "0.0.0"
authors = ["The Rust-Crypto Project Developers"]
license = "MIT/Apache-2.0"

[dependencies]
crypto_hashes = {path = "../crypto-hashes"}
symmetric_ciphers = {path = "../symmetric-ciphers"}

# [features]
# default = ["std"]
# std = []